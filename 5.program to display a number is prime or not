#include<stdio.h>
int main(void)
{
int num,i,flag;
scanf("%d",&num);
for(i=2;i<num/2;i++)
{
 if((num%i)==0)
 {
 	flag=1;
 	break;
 }
 if(flag==0)
 printf("number %d is prime",num);
 else
 printf("number %d is not prime",num);
	return 0;
}
